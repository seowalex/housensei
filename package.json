{
  "name": "housensei",
  "version": "1.0.0",
  "scripts": {
    "build": "(cd frontend && npm ci --production=false && npm run build && npm prune --production); (cd backend && npm ci --production=false && npm run build && npm prune --production)",
    "start": "cd backend && npm start"
  }
}
