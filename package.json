{
  "name": "housensei",
  "version": "1.0.0",
  "scripts": {
    "build": "(cd frontend && npm ci --production=false --ignore-scripts && npm run build); (cd backend && npm ci --production=false --ignore-scripts && npm run build)",
    "start": "cd backend && npm start"
  }
}
